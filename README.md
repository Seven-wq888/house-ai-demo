# 二手房 AI 内容助手 Demo

基于通义千问大模型，帮助房产经纪人一键生成视频口播文案、标题、多平台文案。

## 使用方式

1. 配置 API Key：
   ```bash
   export DASHSCOPE_API_KEY="你的通义千问API Key"
   ```

2. 安装依赖：
   ```bash
   pip install streamlit dashscope
   ```

3. 启动：
   ```bash
   streamlit run app.py
   ```

4. 浏览器会自动打开 http://localhost:8501
